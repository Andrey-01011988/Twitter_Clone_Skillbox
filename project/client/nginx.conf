user  www-data;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;

    server {
        listen 80;
        root /app;  # Установлен корень на /app

        location /static/ {
            alias /app/static/; # Указываем путь к статическим файлам
            autoindex on; # Включаем отображение содержимого директории (опционально)
        }

        location /api/ {
            proxy_pass http://server:5000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        location /index_fastAPI.html {
           alias /app/templates/index_fastAPI.html;
        }

        location / {
            try_files $uri /index_fastAPI.html;
            autoindex on;
        }
    }
}